fastapi>=0.115
uvicorn>=0.34
python-dotenv>=1.0
pydantic>=2.0.0
python-multipart>=0.0.9

# VLM (vision) path only — PDF/image rendering for vlm:: models.
# Remove these two if you never use the image-based extraction path.
pypdfium2>=4.30.0
Pillow>=10.0.0
