services:
  web:
    build:
      context: .
    env_file:
      - .env
    stdin_open: true
    ports:
      - "8000:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    tty: true
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
