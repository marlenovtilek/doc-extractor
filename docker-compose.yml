services:
  web:
    build:
      context: .
    env_file:
      - .env
    stdin_open: true
    ports:
      - "8000:8000"
    tty: true
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
